<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="biz.mapper.biz.equipment.EquipmentMapper">

    <!--조회-->
    <select id="findAll" resultType="biz.equipment.vo.EquipmentVO">
        SELECT id, serial_number, access_number, director_id, director, status
        FROM EQUIPMENT
    </select>

    <!--단건조회    -->
    <select id="findById" resultType="biz.equipment.vo.EquipmentVO">
        SELECT id, serial_number, access_number, director_id, director, status
        FROM EQUIPMENT
        WHERE ID = #{id}
    </select>

    <!--등록    -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO EQUIPMENT(serial_number, access_number, director_id, director, status)
        VALUES ( #{equipment.serialNumber}, #{equipment.accessNumber},
                 #{equipment.directorId},
                 #{equipment.director}, #{equipment.status})
    </insert>

    <!--    수정-->
    <update id="update" parameterType="biz.equipment.vo.EquipmentVO">
        UPDATE EQUIPMENT
        SET SERIAL_NUMBER = #{equipment.serialNumber},
            ACCESS_NUMBER = #{equipment.accessNumber},
            director_id = #{equipment.directorId},
            DIRECTOR      = #{equipment.director},
            STATUS = #{equipment.status}
        WHERE ID = #{equipment.id, jdbcType=BIGINT}
    </update>

    <!--    삭제-->
    <delete id="deleteById" parameterType="long">
        DELETE
        FROM EQUIPMENT
        WHERE ID = #{id,jdbcType=BIGINT}
    </delete>

    <select id="duplicateChkSerialNumber" resultType="String">
        SELECT SERIAL_NUMBER
        FROM EQUIPMENT
        WHERE SERIAL_NUMBER = #{serialNumber}
    </select>

    <select id="duplicateChkAccessNumber" resultType="String">
        SELECT ACCESS_NUMBER
        FROM EQUIPMENT
        WHERE ACCESS_NUMBER = #{accessNumber}
    </select>

    <!--전체 삭제-->
    <delete id="deleteAll">
        DELETE
        FROM EQUIPMENT
    </delete>

<!--    시리얼 번호로 찾기-->
    <select id="selectBySerialNumber" resultType="biz.equipment.vo.EquipmentVO">
        SELECT id
        FROM EQUIPMENT
        WHERE SERIAL_NUMBER = #{serialNumber}
    </select>

</mapper>
